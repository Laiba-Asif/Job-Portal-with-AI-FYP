import React, { Suspense } from "react";
import ResetPassword from "./_ressest-password";

const Page = () => {
  return (
    <Suspense>
      <ResetPassword />;
    </Suspense>
  );
};

export default Page;